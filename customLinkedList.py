#to create our custom linkedlist we need --
# a class for nodes
# two attributes data and next
# a class of linkedlist with an attribute next
# add operation in the list before printing it

class Node:
    def __init__(self,data = None):
        self.data = next
        self.next = None

class linked:
    def __init__(self):
        self.head = None

    def show(self):
        node = self.head
        while node is not None:
            print(node.data)
            node = node.next
        
link =linked()
element = Node('Python')
link.head = element
link.show()


#learn more